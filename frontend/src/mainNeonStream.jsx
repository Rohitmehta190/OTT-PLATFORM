import React from 'react';
import ReactDOM from 'react-dom/client';
import NeonStreamApp from './NeonStreamApp';

console.log('🚀 Starting NeonStream Premium OTT Platform...');

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <NeonStreamApp />
  </React.StrictMode>,
);
